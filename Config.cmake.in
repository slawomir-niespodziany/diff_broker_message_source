@PACKAGE_INIT@

get_filename_component(DIFF_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
list(APPEND CMAKE_MODULE_PATH ${DIFF_CMAKE_DIR})

if(NOT TARGET @PROJECT_NAME@::@PROJECT_NAME@)
    include("${DIFF_CMAKE_DIR}/@PROJECT_NAME@Targets.cmake")
endif()
